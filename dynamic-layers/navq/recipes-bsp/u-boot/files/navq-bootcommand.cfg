CONFIG_LEGACY_IMAGE_FORMAT=y
CONFIG_USE_BOOTCOMMAND=y
CONFIG_SYS_MMC_ENV_DEV=1
CONFIG_BOOTCOMMAND="setenv devtype mmc; for devnum in 0 1; do if ext4load mmc ${devnum}:1 ${loadaddr} /boot.scr; then source ${loadaddr}; fi; done; bootflow scan -l; run bsp_bootcmd"
